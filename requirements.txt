# requirements.txt
# Install everything with:
#   pip install -r requirements.txt
#
# What these packages do:
# - pandas, numpy        : data loading + calculations
# - matplotlib, seaborn  : charts and visualizations
# - scikit-learn         : ML models (LogReg, SVM, Random Forest) + metrics + preprocessing
# - jupyter              : run the notebook locally

pandas
numpy
matplotlib
seaborn
scikit-learn
jupyter